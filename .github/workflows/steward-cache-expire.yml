on:
  push:
jobs:
  steward-cache-expire:
    runs-on: ubuntu-latest
    if: ${{ github.repository_owner == 'xuwei-k' }}
    steps:
    - uses: actions/cache/restore@v4
      with:
        path: scala-steward/workspace
        key: scala-steward-
        restore-keys: |
          scala-steward-
